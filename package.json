{
  "name": "timelock-npm-registry",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "markdown-to-html --source README.md --output public/index.html",
    "deploy": "bun run build && wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.9.9",
    "npm-registry-sdk": "^1.2.1"
  },
  "devDependencies": {
    "markdown-to-html-cli": "^5.0.3",
    "wrangler": "^4.40.2"
  }
}
